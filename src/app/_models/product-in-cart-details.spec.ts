import { ProductInCartDetails } from './product-in-cart-details';

describe('ProductInCartDetails', () => {
  it('should create an instance', () => {
    expect(new ProductInCartDetails()).toBeTruthy();
  });
});
